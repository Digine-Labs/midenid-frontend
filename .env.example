# Guardian (PSM) operator endpoint for guardian-protected (multisig) accounts.
# Optional: defaults to OpenZeppelin's operator when unset. Override only if your
# wallet registered its account with a different guardian operator.
VITE_GUARDIAN_ENDPOINT=https://guardian.openzeppelin.com
